// Placeholder QUIC implementation (use quinn or similar crate later)

pub fn send_quic(_addr: &str, _data: &[u8]) {
    println!("QUIC send (stub)");
}

pub fn receive_quic() {
    println!("QUIC receive (stub)");
}
